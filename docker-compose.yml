services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quintaedizione-app
    environment:
      - PORT=${APP_PORT:-8000}
      - ENVIRONMENT=${ENVIRONMENT:-development}
    ports: ["${APP_PORT:-8000}:${APP_PORT:-8000}"]
    restart: unless-stopped
